# Python toolchain — dbt + Great Expectations (local)
# Install: python3.12 -m venv .venv && source .venv/bin/activate && pip install -r toolchain/python-requirements.txt
# Pins: see .local/STACK-VERSIONS.md

dbt-core==1.11.11
dbt-duckdb>=1.9.0
great-expectations==1.18.1
deltalake==1.6.0
