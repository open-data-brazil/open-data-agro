# Python ML extras — Phase 31 LightGBM / SHAP (research early-warning).
# Install: pip install -r toolchain/python-requirements.txt -r toolchain/python-requirements-ml.txt
# Pins: see .local/STACK-VERSIONS.md

numpy>=1.26.0
lightgbm>=4.0.0
shap>=0.44.0
