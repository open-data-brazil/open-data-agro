version: 2

models:
  - name: mart_conab__estimativa_graos
    description: |
      Business mart — grain is produto + uf + safra (one row per crop, state, and season).
      Materialized to lake/gold/mart_conab__estimativa_graos/ as Parquet (local-first).
    config:
      meta:
        dataset_id: conab.estimativa-graos
        grain: produto, uf, safra
    columns:
      - name: produto
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: safra
        tests:
          - not_null
      - name: capturado_em
        description: Capture timestamp (_ingested_at from silver).
        tests:
          - not_null
      - name: fonte_oficial
        description: CONAB portal URL (required citation).
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - uf
              - safra

  - name: mart_conab__serie_historica_graos
    description: |
      Business mart — grain is produto + uf + ano (historical series).
      Materialized to lake/gold/mart_conab__serie_historica_graos/ as Parquet (local-first).
    config:
      meta:
        dataset_id: conab.serie-historica-graos
        grain: produto, uf, ano
    columns:
      - name: produto
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: ano
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - uf
              - ano

  - name: mart_conab__oferta_demanda
    description: |
      Business mart — supply/demand balance grain is produto + safra.
      Materialized to lake/gold/mart_conab__oferta_demanda/ as Parquet (local-first).
    config:
      meta:
        dataset_id: conab.oferta-demanda
        grain: produto, safra
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: safra
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - safra

  - name: mart_conab__precos_semanal_uf
    description: |
      Business mart — weekly ag prices by UF.
      Grain is produto + id_produto + uf + ano + mes + semana + nivel_comercializacao.
    config:
      meta:
        dataset_id: conab.precos-agropecuarios-semanal-uf
        grain: produto, id_produto, uf, ano, mes, semana, nivel_comercializacao
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: valor_produto_kg
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - id_produto
              - uf
              - ano
              - mes
              - semana
              - nivel_comercializacao

  - name: mart_conab__precos_semanal_municipio
    description: |
      Business mart — weekly ag prices by municipality.
      Grain is produto + id_produto + cod_ibge + ano + mes + semana + nivel_comercializacao.
    config:
      meta:
        dataset_id: conab.precos-agropecuarios-semanal-municipio
        grain: produto, id_produto, cod_ibge, ano, mes, semana, nivel_comercializacao
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: cod_ibge
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: valor_produto_kg
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - id_produto
              - cod_ibge
              - ano
              - mes
              - semana
              - nivel_comercializacao

  - name: mart_conab__precos_mensal_uf
    description: |
      Business mart — monthly ag prices by UF.
      Grain is produto + id_produto + uf + ano + mes + nivel_comercializacao.
    config:
      meta:
        dataset_id: conab.precos-agropecuarios-mensal-uf
        grain: produto, id_produto, uf, ano, mes, nivel_comercializacao
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: valor_produto_kg
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - id_produto
              - uf
              - ano
              - mes
              - nivel_comercializacao

  - name: mart_conab__precos_mensal_municipio
    description: |
      Business mart — monthly ag prices by municipality.
      Grain is produto + id_produto + cod_ibge + ano + mes + nivel_comercializacao.
    config:
      meta:
        dataset_id: conab.precos-agropecuarios-mensal-municipio
        grain: produto, id_produto, cod_ibge, ano, mes, nivel_comercializacao
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: cod_ibge
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: valor_produto_kg
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - id_produto
              - cod_ibge
              - ano
              - mes
              - nivel_comercializacao

  - name: mart_conab__precos_minimos
    description: |
      Business mart — CONAB minimum prices by UF and vigency period.
      Grain is produto + id_produto + uf + regionalizacao + vigency start/end.
    config:
      meta:
        dataset_id: conab.precos-minimos
        grain: produto, id_produto, uf, regionalizacao, ano_inicio_vigencia, mes_inicio_vigencia, ano_termino_vigencia, mes_termino_vigencia
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: uf
        tests:
          - not_null
      - name: preco
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - produto
              - id_produto
              - uf
              - regionalizacao
              - ano_inicio_vigencia
              - mes_inicio_vigencia
              - ano_termino_vigencia
              - mes_termino_vigencia

  - name: mart_conab__frete
    description: |
      Business mart — CONAB freight routes by origin/destination municipality.
      Grain is fonte + cod_ibge_origem + cod_ibge_destino + ano + mes.
    config:
      meta:
        dataset_id: conab.frete
        grain: fonte, cod_ibge_origem, cod_ibge_destino, ano, mes
        conab_section: Armazenamento e Logística
    columns:
      - name: cod_ibge_origem
        tests:
          - not_null
      - name: cod_ibge_destino
        tests:
          - not_null
      - name: uf_origem
        tests:
          - not_null
      - name: uf_destino
        tests:
          - not_null
      - name: valor_frete_tonelada
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - fonte
              - cod_ibge_origem
              - cod_ibge_destino
              - ano
              - mes

  - name: mart_conab__capacidade_estatica
    description: |
      Business mart — static storage capacity by UF and year (000 t).
      Grain is ano + uf.
    config:
      meta:
        dataset_id: conab.serie-historica-capacidade-estatica
        grain: ano, uf
        conab_section: Armazenamento e Logística
    columns:
      - name: uf
        tests:
          - not_null
      - name: ano
        tests:
          - not_null
      - name: quantidade_mil_t
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ano
              - uf

  - name: mart_conab__prohort_diario
    description: |
      Business mart — CONAB Prohort daily CEASA horticulture prices.
      Grain is municipio_ceasa + cod_ibge_municipio + ceasa + produto + unidade_medida + data_preco.
    config:
      meta:
        dataset_id: conab.prohort-diario
        grain: municipio_ceasa, cod_ibge_municipio, ceasa, produto, unidade_medida, data_preco
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: cod_ibge_municipio
        tests:
          - not_null
      - name: preco_diario
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - municipio_ceasa
              - cod_ibge_municipio
              - ceasa
              - produto
              - unidade_medida
              - data_preco

  - name: mart_conab__prohort_mensal
    description: |
      Business mart — CONAB Prohort monthly CEASA horticulture trade.
      Grain is ano + mes + cod_ibge_municipio_ceasa + ceasa + produto + cod_ibge_municipio_origem + pais_origem.
    config:
      meta:
        dataset_id: conab.prohort-mensal
        grain: ano, mes, cod_ibge_municipio_ceasa, ceasa, produto, cod_ibge_municipio_origem, pais_origem
        conab_section: Mercado
    columns:
      - name: produto
        tests:
          - not_null
      - name: cod_ibge_municipio_ceasa
        tests:
          - not_null
      - name: qtd_comercializada_kg
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ano
              - mes
              - cod_ibge_municipio_ceasa
              - ceasa
              - produto
              - cod_ibge_municipio_origem
              - pais_origem

  - name: mart_conab__operacoes_comercializacao
    description: |
      Business mart — CONAB commercialization operations (auctions / private sales).
      Grain is id_edital + num_lote + num_dco + produto + id_produto + operacao + ano_edital + mes_edital.
    config:
      meta:
        dataset_id: conab.operacoes-comercializacao
        grain: id_edital, num_lote, num_dco, produto, id_produto, operacao, ano_edital, mes_edital
        conab_section: Abastecimento
    columns:
      - name: produto
        tests:
          - not_null
      - name: id_edital
        tests:
          - not_null
      - name: uf_armazem_origem
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - id_edital
              - num_lote
              - num_dco
              - produto
              - id_produto
              - operacao
              - ano_edital
              - mes_edital

  - name: mart_conab__vendas_balcao
    description: |
      Business mart — CONAB counter sales by municipality and month.
      Grain is ano + mes + municipio_armazem_venda + uf.
    config:
      meta:
        dataset_id: conab.vendas-balcao
        grain: ano, mes, municipio_armazem_venda, uf
        conab_section: Abastecimento
    columns:
      - name: uf
        tests:
          - not_null
      - name: municipio_armazem_venda
        tests:
          - not_null
      - name: qtd_produto_kg
        tests:
          - not_null
      - name: capturado_em
        tests:
          - not_null
      - name: fonte_oficial
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ano
              - mes
              - municipio_armazem_venda
              - uf
